# Docusaurus build artifacts & caches (regeneratable with npm install/build)
docs-site/node_modules/
docs-site/build/
docs-site/.docusaurus/
docs-site/.cache-loader/
docs-site/package-lock.json

# OS metadata
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# IDE & editor configs
.vscode/
.idea/
*.swp
*.swo
*~

# Design working directory (internal, development only)
docs/Design/

# Live scan state (tenant-specific; never commit to the public repo)
inventory/

# Preview-script outputs (regeneratable via tests/scripts/Preview-*.ps1)
tests/output/

# Environment variables (if used)
.env.local
.env.*.local

